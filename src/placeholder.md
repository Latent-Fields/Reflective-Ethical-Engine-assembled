# Source placeholder

This repository is specification-first.

Add reference implementations here as they emerge, e.g.:

- `src/ree/` core interfaces
- `src/envs/` environment adapters
- `src/eval/` evaluation harness
